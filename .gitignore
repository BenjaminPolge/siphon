# Local coding-agent artifacts
.claude/
CLAUDE.local.md

# IDEs
.idea/

# macOS
.DS_Store

# Secrets — setup writes an API key into host config, never into the repo
.env
.env.*
*.key
*.pem
.claude/settings.local.json
.cursor/
.codex/
